# 重装归途与末日风暴适配

日期：2026-09-08。两款游戏已完成适配与支持项实机验收。

## 已确认范围

- 目标：`D:\Downloads\RPG\重装归途-0.1.33`、`D:\Downloads\RPG\末日风暴-1.9.6`。
- 如果目标游戏使用 Themida 壳，停止该目标的适配并记录识别证据。
- 按工具箱现有能力完整适配：启动并注入、数据读取、修改器、存读档、地图与事件工具；逐项实测并明确记录引擎限制。用户已确认此验收范围。
- 沿用项目现有的当前游戏、桥接会话、影子目录及存档槽位等用语；实机确认重装归途使用浏览器存档后，已在 CONTEXT.md 补充该用语。无需记录不可逆架构选择的 ADR。

## 静态检查结果

- 重装归途：入口 `重装归途.exe`，x64 Tauri/WebView2；实机确认 MV 1.6.1，扫描已修正为 MV/MZ。使用独立 WebView2 测试配置及浏览器存档。未发现 Themida 正向证据。
- 末日风暴：x86 NW.js / MV 1.6.1，存在 `grover-boot` 指纹。未发现 Themida 正向证据。已修复 `notification_helper.exe` 导致入口歧义的问题，通过裸启动及原生注入建立文件通道。
- 以上是静态证据，不把未命中字符串视为严格排除全部隐藏保护。后续若确认 Themida，遵照用户要求停止该游戏适配。

## 已确认实施方案

先补入口识别并验证已有接入路线，再围绕真实能力缺口修改；验收覆盖用户确认的现有能力。使用独立测试进度或存档副本验证写入，保留原有进度。最终记录两款游戏逐项结果、引擎限制、回归结果，并生成本地修复包。

## 实施状态

- 已修复重命名 NW.js 游戏与 `notification_helper.exe` 同目录时的入口识别，保留多个真实入口的歧义拒绝。
- Grover 扫描输出与实际裸启动加注入路线保持一致。
- 回归测试先复现入口为 `null` 的失败，修复后通过。
- 两款游戏分别使用隔离副本执行真实运行时验收；原游戏不写入测试进度。
- 重装归途实机为 MV 1.6.1，Tauri 静态扫描已改为 MV/MZ。TH 重命名引擎通过动态别名接入，保持新游戏和读档后的对象替换语义。
- 已补浏览器存档列表、存读档、备份恢复和删除；备份只包含游戏槽位及其索引，不包含全部浏览器数据。专用回归通过。
- 重装归途金币、库存、变量采用编码字段。读写和数值锁优先使用原生解码/写入方法；已知编码容器禁止裸字段回退（包括空物品槽），金币锁尊重原生上限。普通数值容器仍兼容旧游戏的空实现回退。
- TH_ItemCore 的真实库存是角色背包及仓库，已通过专用适配聚合查询并逐件调用游戏的 `newGetItem`/所有者删物品接口。独立装备保留实例 ID；从基础装备目录删除改造实例、锁定装备、把独立弹仓当普通物品修改均明确拒绝。普通物品锁在提交前验证，批量恢复锁失败时保留原有整组设置。
- `actor.param.add` 对游戏拒绝的属性变化返回明确错误；重装归途的原生 `addParam` 只刷新而不修改属性，不宣称该功能已生效。
- 清敌同时使用原生 HP setter 与死亡状态接口，并验证 `isAlive`。兼容 `die()` 只清状态而不清 HP、普通 `addState(1)` 被游戏拒绝的情况；必要时走原生 `addNewState`，不直接改状态数组。两种缺口均先复现回归失败再修复，末日风暴实机确认 HP=0 且死亡。
- 全量测试的前半段与后续续跑均通过，覆盖原有引擎适配；浏览器 56 组页面/主题/尺寸及新增浏览器存档异步备份交互通过。
- 2026-09-08 完整 `npm test` 在 0.7.8 版本单次执行通过，已包含专用背包、锁原子提交、金币封顶/编码空槽和小数变量锁规范化回归。RGSS v3 真实档案样本测试因未提供样本跳过，其余现有测试通过。
- 重装归途最终实机专项：库存/含独立装备存档 9/9，地图/事件/真实战斗 11/11；测试副本及其 WebView2 子进程已关闭。详见 `HOMECOMING-0133-ACCEPTANCE.md`。
- 文件通道命令 ID 增加工具箱服务实例命名空间，修复重启工具箱后首个 `f1` 与活游戏已处理命令撞号、导致命令超时的问题。两实例接续同一真实文件通道的回归先复现失败，修复后文件通道及会话/WS 回归全部通过；重新附加仍保留旧队列。
- 末日风暴的 FT 重命名全局通过动态别名接入；保留游戏已有的标准全局。回归覆盖真实对象解析和新游戏/读档后对象替换。正式冷启动的只读验收通过 10 类目录及图标表：物品 610、武器 750、防具 750、技能 400、状态 65、角色 31、敌人 175、敌群 159、地图目录 368、公共事件 80。
- 末日风暴原生金币和经验接口需要第二参数 `true`。已为工具箱命令按 FT 协议调用，保持原生上限、通知和升级处理；金币原生拒绝的部分不再裸字段补写。战斗倍率保留游戏调用的完整参数。
- 末日风暴基础及存档最终 14/14 通过，包括原生金币/经验、装备库存、参数、技能状态、移动、锁、文件存读档与数据树应用。独立状态添加/移除命令先调用原生 `clearResult`，修复上一操作的 `removedStates` 阻止再次添加的问题，保留游戏抗性规则；命令回归先红后绿。
- 末日风暴地图/事件/战斗最终 10/10 通过：真实地图网格与事件读取、原生解释器执行、跨图/同图传送、菜单、生命锁/无消耗、合法地图遇敌、金币/经验/掉落倍率、无敌、一击必杀及清敌。使用 Map018 的实际背景 14/20 和原生遇敌；实验室 Map004 引用缺失背景，已给测试增加资源预检查，未补写占位图。
- 额外验证原生胜利结算：基础金币 30 / 经验 60，在倍率 2 时实际入账金币 60 / 经验 120，跨级经验账也一致，没有二次放大。游戏原生结算文字仍显示基础奖励，属于该自定义界面的显示限制。
- 用户补充末日风暴出现原生变速警告，且设置中没有帧率选项。原生 `playtime` 使用渲染帧数 / 60；修复前 2001 ms 渲染 284 帧。新增仅匹配 FT 全局及启用 TH-BaseSet/TDDP 的 60 FPS 调度，保留原生齿轮 2、1/120 秒逻辑步长、累积器和时间检测。修复后实机 2000 ms 渲染 120 帧；正式冷启动连续 95 秒、19 个前台样本均正常响应，没有首次警告。回归覆盖 30/60/120/144/240 Hz、停止/恢复及非目标游戏。
- 末日风暴原生 `updateMain` 不接受时间参数，工具箱现有额外加速接口无效果，因此明确拒绝开启该项；不把设置回读当成加速成功。该拒绝在任何其他选项写入前验证，并有原子性回归。
- 集成最终 FT 别名、限帧、金币/经验协议、死亡状态、独立状态操作与启动兼容后，完整 `npm test` 退出 0；记录于 `tmp/new-games-final-tests.log`。未提供的 RGSS v3 真实档案样本仍明确跳过。
- 末日风暴原版在本机无注入启动也会约 15 秒退出，已定位到 TH-QianC 的模块检查误判 Sangfor。仅针对已确认安装路径的组件报告做兼容，保留组件运行；正式启动在数据库和真实场景就绪后立即安装桥接，避免桥前高刷计时。启动流程 47 项回归通过，并重复完成真实冷启动。

## 原始入口完整性

两款原始 EXE 与隔离副本中未修改的原始入口 SHA-256 一致。重装归途 CDP 参数补丁写到另一个 `.rmch-cdp.exe` 文件。

- 重装归途：`0ce495b32931ea3095bd819081c04a9966c02fcb4249496f731c1c738247ac68`
- 末日风暴：`45e2759a6d406849ebc953cdbeb65a510525e47cd8c3a5c1bee209f29113ea11`
