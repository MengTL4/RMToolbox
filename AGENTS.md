# RMToolbox

面向本地单机 RPG Maker 游戏的运行中修改工具。领域词汇见根目录 `CONTEXT.md`，架构决策见 `docs/adr/`。

## Agent skills

### Issue tracker

Issues 和 spec 以 GitHub Issues 形式跟踪（`MengTL4/RMToolbox`，用 `gh` CLI 操作）。See `docs/agents/issue-tracker.md`.

### Triage labels

使用五个默认标签：`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`。See `docs/agents/triage-labels.md`.

### Domain docs

单上下文布局：根目录一份 `CONTEXT.md` + `docs/adr/`。See `docs/agents/domain.md`.
