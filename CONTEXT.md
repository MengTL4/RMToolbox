# RM 工具箱

面向本地单机 RPG Maker 游戏的运行中修改工具。领域用语如下。

## 用语

**当前游戏**：用户在工具箱中选定的操作对象，修改器、数据、存档和控制台共同针对它。连接断开不代表用户改选了其他游戏。

**启动并注入**：由工具箱启动游戏，并建立可操作游戏的桥接会话。

**附加**：为已经运行的游戏建立桥接会话；部分游戏家族需要通过接管重启才能完成。

**接管重启**：结束目标游戏的现有实例，再由工具箱重新启动该游戏以建立桥接会话。

**桥接会话**：工具箱与某个运行中游戏之间的操作联系，承载游戏状态和命令结果。
_避免_：把游戏进程、游戏目录或游戏存档称为会话。

**游戏壳**：包装游戏运行过程、影响其识别和工具箱接入方式的保护或启动机制。

**影子目录**：供工具箱启动游戏使用的独立运行目录，保留真实游戏目录作为原始来源。

**桥接命令**：工具箱向游戏提出的结构化查询或修改请求。

**存档槽位**：游戏用于保存和选择玩家进度的位置，可以有编号或名称。

**存档文件**：保存玩家进度或存档索引的本地文件。
_避免_：把扩展名相同的游戏数据库、脚本归档称为存档文件。

**浏览器存档**：由游戏的浏览器环境保存的玩家进度或存档索引，以存档槽位呈现，不对应可直接管理的单个存档文件。
_避免_：为浏览器存档虚构磁盘目录，或把浏览器的全部站点数据当作游戏存档。

**影子存档副本**：影子目录中与真实游戏目录的存档对应的另一份玩家进度。

**目录图标**：游戏用来表示物品、装备、技能或状态等目录条目的图片。

**图标表**：将多个目录图标排列在一起、供游戏按位置取用的一张图片。

**当前地图**：当前游戏中玩家此刻所在的地图；在工具箱列表中选中另一张地图不改变当前地图。

**地图事件**：隶属于某张地图、带有位置和事件页的游戏对象，例如人物、宝箱、门或剧情触发点。

**公共事件**：可被游戏其他事件或系统调用的事件指令集合，不以某个地图位置作为身份。

**事件页**：地图事件的一组条件与行为定义；同一事件可以有多页，游戏根据条件决定当前生效页。

**事件迷你地图**：用地图网格呈现玩家、地图事件和通行信息的辅助视图。

**事件解释器**：工具箱中查看事件指令、分支、条件和参数的工具入口，也承载支持范围内的事件步骤执行操作。
_避免_：与游戏内部负责执行指令的运行时解释器混为一谈。

**选步执行**：用户从事件中选取一个完整步骤并发起执行的操作。
_避免_：与从头开始、每次推进一步的顺序单步运行混为一谈。

**事件步骤**：供用户选取执行的完整事件行为单元；一段对话包含其续行，选项、条件分支和循环包含其完整结构，公共事件调用包含被调用事件的运行。
_避免_：将每一行指令都视为可独立执行的步骤。
